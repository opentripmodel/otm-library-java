package otm.model.entities;

/**
 * Defines categories for transport. The specific meaning of each category (One, Two, Three, Four)
 * would be defined by the OpenTripModel specification.
 */
public enum TransportCategory {
    /**
     * Transport Category One.
     */
    ONE,
    /**
     * Transport Category Two.
     */
    TWO,
    /**
     * Transport Category Three.
     */
    THREE,
    /**
     * Transport Category Four.
     */
    FOUR
}